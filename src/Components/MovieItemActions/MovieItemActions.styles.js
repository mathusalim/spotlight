import styled from "styled-components";

const StyledActionContainer = styled.div`
  width: 40%;
  flex: 1;
  display: flex;
  justifycontent: space-evenly;
`;

const StyledActionDivider = styled.div`
  width: 0.5rem;
`;

export { StyledActionContainer, StyledActionDivider };
